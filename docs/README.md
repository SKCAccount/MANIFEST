# Suite documents have moved

`discovery.md` and `suite-design.md` — the estate discovery master file and
the suite identity/authorization design — now live in their own repo:
**[SKCAccount/SEAKING-SUITE](https://github.com/SKCAccount/SEAKING-SUITE)**
(locally: `../SEAKING-SUITE/`). They describe the whole estate, not just
MANIFEST, and suite-wide sessions now open at the `Claude Projects` root.

Their manifest-era history is in this repo's log through `a02ab64`; the
final pre-move revisions live in SEAKING-SUITE's first content commit.
